apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-b-moath
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-b-moath
  template:
    metadata:
      labels:
        app: backend-b-moath
    spec:
      containers:
      - name: backend-b-moath
        image: backend-b
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: webapp-config-moath
        - secretRef:
            name: webapp-secrets-moath
